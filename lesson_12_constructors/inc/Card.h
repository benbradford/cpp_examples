#pragma once

struct Card {

	// no constructors specified, a default constructor will be supplied
	// values will be undefined on construction
	// i.e. Card() { }
	
	char face; // A, 1, 2 .... J, Q, K
	char suit; // H, D, S, C
};